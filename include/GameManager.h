/*#ifndef GAMEMANAGER_H
#define GAMEMANAGER_H

#include "HashTable.h"    // Include the HashTable class definition
#include "Grid.h"         // Include the Grid class definition for the game grid
#include "Dictionary.h"   // Include the Dictionary class for loading words

using namespace std;

// Class definition for GameManager
class GameManager {
public:

    // Method to run the game
    // gridFile: Path to the file containing the grid data
    // dictionaryFile: Path to the file containing the dictionary words
    void runGame(const string& gridFile, const string& dictionaryFile);
};

#endif
*/
