/*#include "GameManager.h"
#include <iostream>
using namespace std;

// Function to run the game using a grid and a dictionary
void GameManager::runGame(const string& gridFile, const string& dictionaryFile) {
    // Create a hash table for storing dictionary words
    HashTable hashTable(1000);           // Initialize the hash table with an appropriate size

    // Load the dictionary into the hash table
    Dictionary::loadDictionary(dictionaryFile, hashTable);

    // Create a grid object with dimensions 10x10
    //Grid grid(13, 13);



    // Find all valid words in the grid using the dictionary stored in the hash table
    //vector<string> foundWords = grid.findWords(hashTable);

    // Display the found words to the user
    //cout << "Found Words:\n";
   // for (const auto& word : foundWords) {
       // cout << word << "\n"; // Print each word in the list of found words
    //}

}
*/
